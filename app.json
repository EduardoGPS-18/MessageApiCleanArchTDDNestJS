{
  "environments": {
    "test": {
      "scripts": {
        "test": "yarn && yarn run test"
      }
    },
    "prod": {
      "scripts": {
        "postdeploy": "yarn && yarn run migration:run"
      }
    }
  }
}